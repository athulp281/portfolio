import React from "react";
import SmokeTitle from "./SmokeTitle";

export default function ProjectTitile() {
    return (
        <div style={{ display: "flex", justifyContent: "center" }}>
            <SmokeTitle title="Projects" />
        </div>
    );
}
